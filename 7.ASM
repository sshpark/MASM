CSEG SEGMENT
	ASSUME CS:CSEG
	START:
		MOV AH, 07H			;输入数字
		INT 21H
		
		CMP AL, 36H
		JE COUT
		JNE EXIT
		COUT:
			MOV DL, AL
			MOV AH, 02H
			INT 21H
			MOV AH, 4CH
			INT 21H
		EXIT:
			MOV AH,4CH
			INT 21H
CSEG ENDS
END START