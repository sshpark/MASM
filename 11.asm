ASSUME CS: CODE, DS: DATA, SS: STACK



CODE SEGMENT
	START:
		MOV AX, STACK
		MOV SS, AX
		MOV SP, 16

		MOV AX, DATA
		MOV DS, AX

		PUSH DS: [0]
		PUSH DS: [2]
		POP  DS: [2]
		POP	 DS: [0]

		MOV AH, 4CH
		INT 21H
CODE ENDS

STACK SEGMENT
	DW 0, 0, 0, 0, 0, 0, 0 
STACK ENDS

DATA SEGMENT
	DW 01234H, 0456H, 0789H, 0ABCH
DATA ENDS

END START