DSEG SEGMENT
	MAX DB	50
	ACT	DB  ?
	STR	DB 	50		DUP(?)
	DIGIT	DB		'At least one DIGIT!','$'
DSEG ENDS

CSEG SEGMENT
	ASSUME CS: CSEG, DS: DSEG
	START:
		MOV 	AX, DSEG
		MOV 	DS, AX
		LEA 	DX, DIGIT
		MOV 	AH, 09H
		INT 	21H
		MOV 	AH, 0AH
		LEA 	DX, MAX
		INT 	21H
		MOV 	AH, 4CH
		INT 	21H
CSEG ENDS
END START