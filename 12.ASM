ASSUME CS: CSEG, DS: DSEG, SS: SSEG

SSEG SEGMENT
	DW 1280H, 1234H
SSEG ENDS

DSEG SEGMENT
	DB 'UNIX'
	DB 'FORK'
DSEG ENDS



CSEG SEGMENT
	START:
		MOV AL, 'A'
		MOV BL, 'B'
		MOV AH, 4CH
		INT 21H
CSEG ENDS
END START
